[tool.ruff]
target-version = "py313"
line-length = 120
extend-exclude = [
    ".venv",
    "data",
    "staticfiles",
]

[tool.ruff.lint]
# Base pragmatica inicial: errores de sintaxis/imports criticos sin convertir
# deuda historica de estilo en bloqueo de CI.
select = ["E9", "F63", "F7", "F82"]
